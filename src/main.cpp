#include <Arduino.h>
#include <Foo.h>
#include <Bar.h>

Bar bar = Bar();
Foo foo = Foo(&bar);

void setup() {
  // put your setup code here, to run once:
}

void loop() {
  // put your main code here, to run repeatedly:
}